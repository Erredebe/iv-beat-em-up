# Placeholders 16-bit

Este directorio contiene assets provisionales creados para el MVP cuando no se integra aun un recurso externo final.

Los sprites actuales se generan en runtime desde `generateTextures.ts` para mantener consistencia pixel-art y evitar dependencia de material con copyright restringido.

